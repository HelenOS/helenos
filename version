VERSION = 0
PATCHLEVEL = 3
SUBLEVEL = 0
#EXTRAVERSION = 0
NAME = Tartare

ifdef EXTRAVERSION
	RELEASE = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL).$(EXTRAVERSION)
else
	RELEASE = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL)
endif
