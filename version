VERSION = 0
PATCHLEVEL = 2
SUBLEVEL = 0
EXTRAVERSION = 5
NAME = Twilight

ifdef EXTRAVERSION
	RELEASE = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL).$(EXTRAVERSION)
else
	RELEASE = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL)
endif
