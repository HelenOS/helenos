VERSION = 0
PATCHLEVEL = 2
SUBLEVEL = 0
EXTRAVERSION = 2
NAME = Daylight

ifdef EXTRAVERSION
	RELEASE = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL).$(EXTRAVERSION)
else
	RELEASE = $(VERSION).$(PATCHLEVEL).$(SUBLEVEL)
endif
